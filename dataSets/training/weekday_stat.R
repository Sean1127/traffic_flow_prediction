fullChart = chart
weekday = //
for(j in 1:7){}
for(i in seq(j,91,7)){
  weekday[j] <- chart[i,];
}
}

for(i in 1:91) {
  for(j){
    if( == NA){
      weekday=date(row(chart)[j])
      var = var(weekday)
      avg 
        if(var <= err) {
          fullChart[j,i] = avg
        }
    }
  }
}